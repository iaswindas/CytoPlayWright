# cypw

`cypw` is an offline-first migration compiler that analyzes and converts enterprise Cypress TypeScript E2E suites into side-by-side Playwright Test output.

## Commands

- `cypw init`
- `cypw analyze`
- `cypw convert`
- `cypw validate`

## Development

```bash
npm install
npm run build
npm test
```
